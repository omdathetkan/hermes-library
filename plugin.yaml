name: bibliotheek
version: "1.1"
description: "Search, check availability, and place holds at your local OCLC WISE library (KB Digital Membership / iWelcome login)."
requires_env:
  - LIBRARY_USERNAME
  - LIBRARY_PASSWORD
